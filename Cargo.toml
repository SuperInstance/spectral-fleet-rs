[package]
name = "spectral-fleet"
version = "0.1.0"
edition = "2021"
authors = ["SuperInstance <dev@superinstance.ai>"]
description = "Spectral methods for fleet matrices — Lanczos iteration, power iteration, spectral clustering"
license = "MIT OR Apache-2.0"
repository = "https://github.com/SuperInstance/spectral-fleet-rs"

[dependencies]
num-traits = "0.2"
rand = "0.8"
thiserror = "1.0"

[dev-dependencies]
approx = "0.5"
proptest = "1.5"
